# file .travis.yml

language: julia

julia:
  - nightly
  - 1.0

os:
  - linux

notifications:
  email: false

env:
  - PYTHON="python"

before_install:
 - (sudo apt-get -qq update && sudo apt-get install -y python python-pip)
 - (sudo pip install Cython ripser)

coveralls: true
